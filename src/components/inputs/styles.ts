/** Shared visual tokens for the Input primitives. Mirrors the inline selects
 *  and option controls used across the app so the new primitives blend in. */
export const inputBase =
  "rounded-lg border border-edge bg-well px-2 py-1 text-sm text-ink focus:border-accent/60 focus:outline-none focus:ring-2 focus:ring-accent/20";
