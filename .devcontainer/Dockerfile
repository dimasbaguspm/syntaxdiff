# SyntaxDiff devcontainer
# Node 24 + pnpm toolchain. oxlint/oxfmt are devDependencies installed by
# pnpm install (setup.sh), so no global install needed here.
FROM mcr.microsoft.com/devcontainers/javascript-node:24

# pnpm is enabled via corepack (postCreateCommand in setup.sh keeps the
# toolchain in sync with the repo's packageManager field).
